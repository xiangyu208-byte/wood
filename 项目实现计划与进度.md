# 木材表面缺陷检测系统实现计划与进度

## 1 项目目标

本项目计划将现有的木材表面缺陷检测原型完善为一个可部署、可复现、可维护的完整系统，并在模型检测、质量评价和数据闭环方面形成可展示、可验证的创新点。

最终目标如下：

- Windows、Linux、macOS 可通过 Docker Compose 一键启动。
- 默认提供 CPU 推理方案，无 CUDA 环境也能运行。
- 提供 NVIDIA GPU 可选加速方案。
- 电脑、手机和平板可通过浏览器使用系统。
- 数据库自动初始化，不要求用户手工建表。
- 模型、上传目录、数据库和端口均通过配置文件或环境变量管理。
- 模型训练、验证、导出和推理流程可以复现。
- 前端、后端、推理服务和数据库具备完整测试与部署文档。
- 至少完成两项具有实验数据支撑的创新功能。

> “任何设备可以运行”在本项目中指：Windows、Linux、macOS 设备可以部署服务，手机和平板可以作为浏览器客户端访问。移动设备本地离线运行模型不作为第一阶段强制目标。

## 2 状态标记说明

- `[x]` 已完成，并已在当前项目中找到对应代码或材料。
- `[-]` 部分完成，已有代码但仍存在问题，或尚未完成验证。
- `[ ]` 未完成，计划新增或重构。
- `[!]` 存在阻塞问题，应优先处理。

## 3 当前项目完成情况

### 3.1 模型与算法

- [x] 已提供 `best.pt` 最终模型权重。
- [x] 最终模型包含 6 个木材缺陷类别。
- [x] 最终权重中包含 C2fPSA 模块信息。
- [x] 已实现 YOLO 图片推理和边界框解析。
- [x] 已返回缺陷类别、置信度和坐标信息。
- [x] 已生成并保存带检测框的结果图片。
- [x] 已提供与最终权重一致的 6 类训练入口、C2fPSA 结构和训练参数配置。
- [x] 数据集 YAML 已统一为最终 6 类，并使用可移植的项目相对路径。
- [-] 已构建不含髓心破坏的 12 类扩展数据集与训练配置；当前部署权重仍为 6 类，尚未完成 12 类重训、复评和替换。
- [x] Python 推理模型路径已改为环境变量，CPU 默认使用 `best.onnx`，GPU 使用 `best.pt`。
- [x] 已恢复原 6 类公开训练/验证数据，并汇总为 5857 张、16946 个目标的 12 类数据集；来源、许可、类别映射和校验报告均已留档。
- [x] 训练脚本已移除 `obj`、`class_weights` 等不受当前 Ultralytics 支持的参数。
- [x] 已提供独立的 `evaluate.py` 验证脚本，可输出指标 JSON、混淆矩阵、PR/F1 曲线。
- [x] 已提供动态输入 `best.onnx`，并通过 ONNX Runtime CPU 的 512、640、896 三种尺寸前向验证。
- [x] 已提供 `benchmark.py` 和 CPU 烟雾基准报告。
- [-] 已记录权重内训练参数、历史指标、模型/ONNX 哈希和导出环境；数据集已恢复，但尚未重新执行训练与独立复评。

### 3.2 Python 推理服务

- [x] 已使用 FastAPI 封装推理服务。
- [x] 已提供 `/health` 健康检查接口。
- [x] 已提供 `/predict` 推理接口。
- [x] 已支持 CPU 与 CUDA 设备选择。
- [x] Python 文件语法检查通过。
- [x] 模型路径、上传目录、访问前缀、设备和推理参数已改为环境变量。
- [-] 宿主机 Anaconda 的 PyTorch 仍存在 DLL 加载问题；已在 Docker CPU 环境完成 PyTorch 与 ONNX 真实图片推理烟雾测试。
- [x] 已提供推理服务独立 `requirements.txt`。
- [x] 已提供 CPU 与 NVIDIA GPU 两套 Docker 运行环境。
- [x] 后端已配置连接/读取超时与重试；推理服务已增加可配置并发闸门、等待超时、统一错误 JSON，并限制只能读取上传根目录内的真实文件。
- [x] 已增加 6 个切片覆盖、坐标还原、按类别 NMS、自适应决策和疑似异常候选单元测试。
- [x] 推理响应已记录模型权重版本；默认使用模型文件名与 SHA-256 前 12 位生成稳定版本标识。

### 3.3 Spring Boot 后端

- [x] 已实现单张图片上传识别接口。
- [x] 已实现批量图片上传识别接口。
- [x] 已实现摄像头图片识别接口。
- [x] 已实现历史记录查询和详情接口。
- [x] 已实现按图片名称、状态、批次、来源、缺陷和时间筛选。
- [x] 已实现单条删除、勾选批量删除和按条件删除。
- [x] 已实现 CSV 导出。
- [x] 已实现 Excel 导出。
- [x] 已实现原图和结果图 ZIP 下载。
- [x] 已建立 `detect_record` 和 `detect_detail` 实体结构。
- [x] 后端已通过 Maven Wrapper 单元测试、可执行 JAR 打包和 Java 17 Docker 镜像构建验证。
- [x] 已使用 Flyway V1 至 V6 管理检测记录、明细、批次、推理参数、自适应模式元数据、质量评价快照以及人工复核标注。
- [x] 数据库用户名、密码和连接地址已移出源码固定值，支持环境变量配置。
- [x] Python 调用异常时记录会从 `PROCESSING` 更新为 `FAIL` 并保存失败原因。
- [x] 业务异常已使用正确 HTTP 状态码和统一错误结构。
- [x] 上传文件使用 UUID 存储，同名图片不会覆盖。
- [x] 历史记录已使用 MyBatis-Plus 数据库原生分页。
- [x] 已校验扩展名、声明 MIME、文件签名、图片解码和尺寸/像素限制。
- [-] 后端现有 15 个单元测试并完成 Docker 冒烟验收，完整数据库集成测试仍在第八阶段补充。
- [x] 已提供 Maven Wrapper 3.9.9 和 Spring Boot 可执行 JAR 构建流程。
- [x] 已通过 Flyway V5 保存质量分、质量等级、面积占比、类别统计、扣分原因和规则版本，并支持历史记录自动补评分。
- [x] 已支持按质量等级及分数区间查询、删除和导出，CSV/Excel 包含完整质量评价字段。
- [x] 已实现人工复核事务、原始模型框与人工标注分离保存、低置信度队列、YOLO 数据包导出和模型版本反馈指标。

### 3.4 Vue 前端

- [x] 已实现批量图片选择和识别页面。
- [x] 已实现摄像头打开、拍照和识别页面。
- [x] 已实现检测结果图片和缺陷明细展示。
- [x] 已实现历史记录列表、筛选和分页界面。
- [x] 已实现历史详情页面。
- [x] 已实现删除和导出操作入口。
- [x] 前端执行 `npm run build` 可以成功构建。
- [x] 前端根据 `PENDING / PROCESSING / SUCCESS / FAIL / PARTIAL_FAIL / CANCELLED` 显示独立文字和语义样式，失败不会误报成功。
- [x] 前端 API 已统一使用相对路径 `/api`，图片使用同源 `/static` 路径。
- [x] ZIP 图片导出已传递“是否有缺陷”筛选条件，并与查询、删除、CSV、Excel 共用后端筛选逻辑。
- [x] 已增加统一 Axios 响应拦截器，并区分业务错误、超时、断网、503、服务器错误和主动取消。
- [x] 已接入异步批量任务，显示总进度、逐张状态、失败原因、取消和重试入口。
- [x] 已完善加载、空数据、超时、网络中断和服务不可用页面状态及恢复操作。
- [x] 已完成桌面、平板和手机响应式布局，并使用 Playwright 在 390×844 视口验证检测与历史页面。
- [-] 已增加 6 个前端状态、质量评价与复核元数据单元测试，并完成桌面/390px 移动端 Playwright 真实浏览器验收；完整组件及自动化端到端测试留待第八阶段。
- [x] 已清理未使用的默认 Vue 模板页面和组件。
- [x] 摄影资源由约 27.9 MB 压缩为约 247 KB WebP；路由懒加载并按需注册 Element Plus，最大 JavaScript 包约 431 KB，生产构建无超大资源警告。
- [x] 识别结果与历史详情已展示质量分、A/B/C/D 等级、缺陷覆盖率、各类别数量和逐项扣分原因；历史列表支持等级及分数区间筛选。
- [x] 历史详情支持确认正确、标记错误、修改类别/坐标、删除误检框和补充漏检框；历史页支持待复核队列、复核状态、模型版本筛选和版本指标对比。

### 3.5 文档与工程交付

- [x] 已有论文文档。
- [x] 已有答辩 PPT。
- [x] 论文和 PPT 中已有系统界面截图。
- [-] 已描述系统架构，但尚未形成正式部署手册。
- [-] 论文已统一使用检查点保存的 mAP50 0.95145 并标注尚未独立复评；本地参考 PPT 按“不上传参考 PPT”的要求保持在 Git 外，仍待作者同步。
- [x] 论文、权重、训练配置、数据 YAML、推理服务和前端已统一为 6 类。
- [x] 已新增项目根目录 README，包含运行、配置、持久化和故障排查说明。
- [x] 已新增 `.env.example` 环境变量模板。
- [x] 已新增 CPU 默认编排和 NVIDIA GPU 覆盖编排文件。
- [x] 已新增 Windows PowerShell 与 Linux/macOS Shell 启停脚本。
- [x] 已提供 Swagger/OpenAPI 在线 API 文档和独立模型报告；系统级测试报告在第八阶段补充。
- [x] README 与环境变量模板已补充可解释质量评分公式、默认权重、等级阈值和“非行业强制标准”声明。
- [x] README 已补充检测、自动入队、人工复核、YOLO 导出、再训练、独立验证和新版本部署的主动学习操作链路。
- [x] 已建立 Git/GitHub 版本管理和 GitHub Actions 持续集成，自动执行后端、前端、Python 测试、前端构建/生产依赖审计及 Compose 配置校验。

## 4 目标技术架构

```text
浏览器 / 手机 / 平板
        |
        v
Nginx + Vue 前端
        |
        | /api 统一反向代理
        v
Spring Boot 后端
   |-- 上传、校验与文件管理
   |-- 识别任务与历史记录
   |-- 质量评分与报表导出
   |-- 推理服务调用与异常处理
        |
        v
FastAPI 推理服务
   |-- ONNX Runtime CPU 模式
   |-- PyTorch CUDA GPU 模式
   |-- 普通推理与精细切片推理
        |
        v
MariaDB + 持久化文件卷
```

## 5 项目实施计划

### 5.1 第一阶段 工程标准化与跨平台运行

优先级：P0  
预计时间：3 至 4 个工作日

#### 计划完成内容

- [x] 整理项目目录结构。
- [x] 删除无用 Vue 模板、IDE 配置和重复编译产物。
- [x] 创建项目根目录 README。
- [x] 创建 `.env.example`。
- [x] 将模型路径、上传目录、端口和数据库配置改为环境变量。
- [x] 创建 Spring Boot 开发、Docker 和生产环境配置。
- [x] 将前端 API 地址改为相对路径 `/api`。
- [x] 通过 Nginx 统一代理前端、后端和静态资源。
- [x] 创建前端 Dockerfile。
- [x] 创建后端 Dockerfile。
- [x] 创建 Python CPU Dockerfile。
- [x] 创建 Python GPU Dockerfile。
- [x] 创建 `docker-compose.yml`。
- [x] 为前端、后端、推理服务和数据库增加健康检查。
- [x] 增加 Windows PowerShell 启停脚本。
- [x] 增加 Linux/macOS Shell 启停脚本。
- [x] 明确持久化数据卷和模型文件挂载方式。

#### 阶段验收条件

- [x] Windows 已通过 `docker compose up -d --build` 重建并启动 CPU 四服务，全部健康。
- [-] Linux 使用相同编排与启动脚本，尚待 Linux 实机验收。
- [-] macOS Intel 和 Apple Silicon 使用多架构 CPU 镜像，尚待对应设备实机验收。
- [x] 数据库、推理、后端和前端四项健康检查均已通过运行态验证。
- [x] 前端生产构建、Nginx 统一入口、API 与静态资源代理均已通过容器实跑验证。
- [x] MariaDB 与上传目录命名卷已通过“识别记录和图片写入、四服务重启、记录和图片仍可读取”的持久性验证。

#### 实施记录和需要改善的内容

> 在实施过程中填写实际完成情况、遇到的问题、采用的方案和后续改善内容。

- 完成日期：`2026-09-24`
- 负责人：`Codex（工程改造）`
- 实际改动：`完成配置环境变量化；新增 dev/docker/prod 后端配置、CPU/GPU 推理镜像、前后端镜像、Nginx 同源代理、Compose 编排、四服务健康检查、数据库初始化 SQL、跨平台启停脚本、根目录 README 和忽略规则；清理 IDE、依赖、构建缓存及未使用 Vue 模板；修复前端依赖漏洞。`
- 遇到的问题：`Docker Desktop 曾因 dockerInference 管道错误不可用，现已恢复；本轮实跑还发现已删除的静态图片被全局异常处理器错误映射为 HTTP 500，以及后端、推理和数据库调试端口默认监听所有网卡。`
- 解决方案：`完成 CPU 四服务重建与运行验收；将缺失静态资源显式映射为 404；默认仅让前端监听 0.0.0.0，后端、推理和数据库调试端口绑定 127.0.0.1；使用共享 uploads 命名卷和只读模型挂载。`
- 仍需改善：`在 Ubuntu、NVIDIA、macOS Intel/Apple Silicon 和移动设备上继续执行实机矩阵；Windows CPU 验收已经完成。`
- 验收结果：`第一阶段工程实现项 17/17 完成；Windows CPU 下四服务健康、Nginx、真实上传识别、历史查询、图片访问、删除清理和重启持久性通过，其他平台仍待实机验证。`

### 5.2 第二阶段 数据库与后端可靠性改造

优先级：P0  
预计时间：4 至 5 个工作日

#### 计划完成内容

- [x] 使用 Flyway 创建数据库初始化和版本迁移脚本。
- [x] 为上传文件生成 UUID，避免同名文件覆盖。
- [x] 校验文件扩展名、MIME 类型、图片格式和图片尺寸。
- [x] 规范推理成功、失败和处理中状态转换。
- [x] Python 服务异常时将记录更新为 `FAIL`。
- [x] 使用正确的 HTTP 状态码和统一错误结构。
- [x] 将历史查询改为数据库分页。
- [x] 统一上传、数据库写入和文件删除的事务补偿逻辑。
- [x] 修复所有筛选条件在查询、导出和删除中的一致性。
- [x] 增加推理调用超时和有限次数重试。
- [x] 增加批量识别任务状态和逐项进度。
- [x] 将数据库凭据移出源码。
- [x] 增加后端 API 文档。
- [x] 增加 Maven Wrapper 和可执行 JAR 构建流程。

#### 阶段验收条件

- [x] 新数据库可以自动建立全部表结构。
- [x] 同名文件连续上传不会覆盖历史文件。
- [x] 推理服务关闭时记录最终状态为 `FAIL`。
- [x] 错误请求返回正确 HTTP 状态码。
- [x] 查询、删除、CSV、Excel 和 ZIP 使用相同筛选条件。
- [x] 一万条历史数据下分页查询不需要读取全部记录。

#### 实施记录和需要改善的内容

- 完成日期：`2026-09-24`
- 负责人：`Codex（数据库与后端可靠性改造）`
- 实际改动：`新增 Flyway V1/V2 迁移与 detect_batch 表；使用 UUID 安全存储并校验扩展名、真实文件签名、MIME、图片解码和尺寸；建立 PENDING/PROCESSING/SUCCESS/FAIL 状态闭环；增加推理超时、重试、失败原因、异步批次接口和逐项进度；统一 HTTP 错误结构；改用数据库原生分页；查询、删除与三类导出复用同一筛选构造器；增加文件/数据库删除补偿、Swagger、Maven Wrapper、可执行 JAR 和文件存储单元测试。`
- 遇到的问题：`本机 Java 26 默认使用的旧版 Lombok 无法执行注解处理，首次编译缺失 getter/setter；原 Dockerfile 的 dependency:go-offline 会下载大量无关插件依赖；现有 MariaDB 11.4 高于 Spring Boot 3.3.2 默认 Flyway 的已测试版本范围并产生兼容性提示。`
- 解决方案：`将 Lombok 升级到 1.18.46 并显式配置注解处理器，项目仍以 Java 17 为目标；Maven Wrapper 固定为 3.9.9，Docker 构建改用 Wrapper 和 BuildKit Maven 缓存；在 MariaDB 11.4 新库上实际执行 V1/V2 迁移并确认表结构与应用健康。`
- 仍需改善：`第三阶段将前端接入异步批次进度和失败样式；第八阶段继续补充数据库集成测试、端到端测试和上传目录孤儿文件巡检；后续升级 Spring Boot/Flyway 后消除 MariaDB 11.4 的版本范围提示。`
- 验收结果：`第二阶段计划项 14/14、验收项 6/6 完成。Maven 测试 4/4 通过；Java 17 Docker 镜像构建成功；全新 MariaDB 自动迁移到 v2；验证 400/404/415/503 与 OpenAPI 200；推理服务离线时同步及异步记录均为 FAIL；两张同名图片生成不同 UUID；插入 10001 条历史记录后第 1000 页仅返回 10 条。`

### 5.3 第三阶段 前端体验与生产构建优化

优先级：P1  
预计时间：3 至 4 个工作日

#### 计划完成内容

- [x] 增加统一 Axios 响应和异常拦截器。
- [x] 根据真实业务状态显示成功、失败和处理中样式。
- [x] 显示批量任务总进度、单张状态和失败原因。
- [x] 增加模型模式、置信度阈值和推理精度选项。
- [x] 完善空数据、加载、超时、网络中断和服务不可用页面状态。
- [x] 优化手机和平板布局。
- [x] 压缩背景图片和其他静态资源。
- [x] 使用路由懒加载拆分 JavaScript 包。
- [x] 清理默认 Vue 模板页面和无用组件。
- [x] 优化识别结果中的中文类别名称和置信度展示。
- [x] 增加任务取消、重试和重新识别入口。
- [x] 增加无障碍标签和键盘可操作性。

#### 阶段验收条件

- [x] 后端返回失败时前端不会显示“识别成功”。
- [x] 批量上传可以查看每张图片的处理状态。
- [x] 390×844 移动浏览器视口可以完成图片选择、识别结果查看和历史查询交互。
- [x] 前端生产构建无明显超大静态资源警告。
- [x] 所有路由使用 History fallback，已验证 `/detect`、`/camera-detect`、`/history`、`/history/1` 直接访问均返回 200。

#### 实施记录和需要改善的内容

- 完成日期：`2026-09-24`
- 负责人：`Codex（前端体验与生产构建优化）`
- 实际改动：`重构 Vue 应用外壳和四个业务页面；统一 Axios 成功/异常处理；前端接入异步批次轮询、总进度、逐项状态与失败原因；新增服务端批次取消、失败/取消项重试和单张重新识别；将 FAST/STANDARD/ACCURATE、置信度阈值与 AUTO/FP32/FP16 参数贯通 Vue、Spring Boot 与 FastAPI；增加加载、空数据、断网、超时和服务不可用状态；完成移动记录卡片、单栏图片和触控布局；增加语义状态、跳转主内容、键盘焦点和减少动效；使用路由懒加载与 Element Plus 按需注册；压缩静态图片并清理 Vue 默认资产；新增 PRODUCT.md、DESIGN.md 和前端说明文档。`
- 遇到的问题：`Element Plus 全量注册使首次生产构建生成约 878 KB JavaScript 包；旧 school.JPG 与 straw.jpg 合计约 27.9 MB；Docker Desktop 启动后 Engine API 持续无响应，无法在本轮重新启动完整四服务进行实推理浏览器联调。`
- 解决方案：`改为按需注册实际使用的 Element Plus 组件，路由全部动态导入，最大 JavaScript 包降至约 431 KB；摄影资源缩放并转为 WebP，合计约 247 KB；使用 Playwright 真实 Chromium 和 API 路由模拟验证成功、部分失败、逐项状态、中文类别、置信度、历史移动卡片及路由刷新，后端使用 Maven 测试/打包和 Python 语法编译独立验证。`
- 仍需改善：`第八阶段在 Docker Engine 稳定后补充四服务真实端到端测试，并在 Android、iOS/iPadOS 实机完成触控、摄像头权限和网络切换验收；继续通过按需样式引入降低约 362 KB 的 Element Plus CSS。`
- 验收结果：`第三阶段计划项 12/12、验收项 5/5 完成。前端状态测试 3/3 通过，Vite 生产构建成功且无超大资源警告；Playwright 在桌面和 390×844 移动视口完成主要页面、部分失败结果、逐项进度与历史记录验证；后端测试 4/4 通过并成功生成可执行 JAR。`

### 5.4 第四阶段 模型训练体系统一与跨平台推理

优先级：P0  
预计时间：4 至 6 个工作日

#### 计划完成内容

- [x] 将训练配置统一为最终 6 类缺陷。
- [x] 修复数据集 YAML 中的类别和数据路径。
- [x] 修复训练脚本中的无效参数。
- [x] 增加 C2fPSA 模型结构配置文件，并验证参数量与最终权重一致。
- [x] 核实 Wise-IoU 是否实际使用：源码、配置和权重训练参数均无使用证据。
- [-] 若使用 Wise-IoU，补充源码、配置和对比实验：经核实未使用，本项不适用。
- [-] 若未使用 Wise-IoU，修订论文和 PPT 中的相关描述：论文已修订；参考 PPT 按用户要求不纳入 Git，待作者在本地同步。
- [x] 推理服务从模型元数据读取类别，不再重复硬编码。
- [x] 创建 `train.py` 统一训练入口。
- [x] 创建 `evaluate.py` 验证入口。
- [x] 创建 `export_onnx.py` 导出入口。
- [x] 创建 `benchmark.py` 性能测试入口。
- [x] 导出 ONNX CPU 模型。
- [x] 保留 PyTorch CUDA GPU 模型。
- [-] 已记录模型版本、训练参数、权重历史指标和哈希；原数据集版本不可用，已在版本清单中显式标记。
- [-] 原 6 类验证集已恢复，尚未重新生成混淆矩阵、PR 曲线和 F1 曲线；12 类扩展集中的霉变、虫蛀验证样本仍不足。
- [-] 论文和模型报告已统一检查点指标口径；参考 PPT 与重新评估图表仍待数据/作者处理。

#### 阶段验收条件

- [x] 无 CUDA 环境已使用 ONNX Runtime `CPUExecutionProvider` 完成前向和真实图片推理。
- [x] GPU 编排保留 PyTorch 权重，并支持 `auto` 或显式设备编号切换 CUDA。
- [-] 训练、验证和导出入口均已实现并通过语法/配置检查，数据集也已就绪；12 类训练与验证尚未执行。
- [x] 模型、训练配置、数据 YAML、推理服务、前端和论文中的类别完全一致。
- [-] 原验证集已恢复，但所有论文指标尚未通过评估脚本独立重新生成。

#### 实施记录和需要改善的内容

- 完成日期：`2026-09-25`
- 负责人：`Codex（模型训练体系统一与跨平台推理）`
- 数据集版本：`已恢复 Zenodo 原 6 类数据，并于 2026-09-26 构建 12 类扩展集：5857 张、16946 个目标、train/val/test=4640/930/287；校验报告与来源清单位于 datasets/wood_defect_12class/metadata。`
- 模型版本：`Ultralytics 8.4.21，YOLOv8s-C2fPSA，6 类，9,915,906 参数；best.pt SHA-256 c6b6ba26…fb0535；第五阶段动态 best.onnx SHA-256 d5bd6032…678a17。`
- 训练环境：`检查点记录 device=0、500 epochs、batch=32、imgsz=896、AdamW；原训练硬件未写入检查点。本次在 Python 3.11、Torch 2.6.0 CPU、ONNX 1.23.0、ONNX Runtime 1.30.0 的 Docker 环境完成导出与验证。`
- 实际指标：`检查点历史记录为 Precision 0.95374、Recall 0.89180、mAP50 0.95145、mAP50-95 0.75373；未将这些值冒充本次独立复评结果。第五阶段对当前动态 ONNX 重跑 CPU 烟雾基准：PyTorch 151.98 ms/张，ONNX Runtime 107.07 ms/张（单张论文内嵌木材图片、896、各 3 次；样本无独立标签）。`
- 遇到的问题：`公开数据对霉变和虫蛀覆盖不足；Roboflow 导出需要账号/API key；当前扩展集的 Oak 和 MVTec 来源带非商业许可；宿主机 Anaconda PyTorch 仍存在 c10.dll 加载错误。`
- 仍需改善：`补充霉变、虫蛀的同域人工框标注与独立验证集；为商业用途替换 CC BY-NC/CC BY-NC-SA 来源；训练 12 类权重并运行 evaluate.py 生成 evaluation.json、混淆矩阵、PR/F1 曲线；在 NVIDIA 主机执行 GPU 吞吐与 FP16 验收。`
- 验收结果：`第四阶段 16 项适用实施内容中 12 项完成、3 项部分完成、1 项受数据集阻塞；5 项验收条件中 3 项完成、1 项部分完成、1 项阻塞。ONNX CPU 模型和四个统一入口已交付，科学指标复现不能在缺少原验证集的情况下宣称完成。`

### 5.5 第五阶段 创新功能一 自适应高分辨率小缺陷检测

优先级：P1  
预计时间：3 至 5 个工作日

#### 计划添加内容

- [x] 增加快速整图、自适应和精细切片模式。
- [x] 快速模式使用 512px 整图推理。
- [x] 精细模式对图片执行 896px、20% 重叠切片推理。
- [x] 将局部检测框坐标还原到原图，并完成边界验收。
- [x] 使用 IoU 0.50 的按类别 NMS 合并重复框。
- [x] 根据图片尺寸、首轮是否检出、置信度和目标面积自动选择推理模式。
- [x] 首轮结果仅位于图片边缘时自动执行重叠切片复查，避免单个边缘框让整图提前结束。
- [x] 对模型未覆盖的木材内部明显暗部和空洞补充 `suspected_anomaly` 人工复核候选，并在前端与模型类别、模型置信度明确区分。
- [x] 前端显示请求模式、实际模式、选择依据、耗时和推理区域数。
- [x] 通过 Flyway V4 保存实际模式、耗时、区域数、图片尺寸和检测结果，并纳入 CSV/Excel 导出。
- [!] 原带标签验证集缺失，无法对裂纹、小节等小目标进行有统计意义的专项精度实验。
- [-] 已输出三模式烟雾耗时与检测数对比表；Precision、Recall、mAP 和小目标召回率待原验证集恢复后计算。

#### 创新点实验记录

| 实验项目 | 普通模式 | 精细模式 | 改善情况 |
|---|---:|---:|---:|
| Precision | 待原验证集 | 待原验证集 | 暂不能判定 |
| Recall | 待原验证集 | 待原验证集 | 暂不能判定 |
| mAP50 | 待原验证集 | 待原验证集 | 暂不能判定 |
| mAP50-95 | 待原验证集 | 待原验证集 | 暂不能判定 |
| 小目标召回率 | 待原验证集 | 待原验证集 | 暂不能判定 |
| 平均推理耗时 | 116 ms（快速整图） | 206 ms（精细切片） | +90 ms；单张无独立标签的木材样本，不代表正式性能 |

#### 实施记录和需要改善的内容

- 完成日期：`2026-09-25`
- 实际方案：`FAST 使用 512px 整图；STANDARD 先做 640px 整图，再按最长边、总像素、首轮检出、置信度、目标面积和是否仅边缘命中决定是否切片；ACCURATE 强制切片。模型结果无法覆盖图像内部时，对木材色调图片的封闭暗部和空洞生成独立的疑似异常复核候选。ONNX 已改为动态输入。`
- 切片尺寸：`896×896；边缘切片贴合原图最远边，小图使用单区域。`
- 重叠比例：`20%，可通过 TILE_OVERLAP 环境变量调整。`
- 合并算法：`按类别贪心 NMS，IoU 阈值 0.50；较小框被同类框覆盖 80% 以上时也会合并，避免整图与切片生成嵌套重复框；切片框先加偏移还原到原图坐标。`
- 样本说明：`已撤销动漫占位图的测试结论，改用从项目论文文档历史识别截图“原图”区域裁取的木材缺陷样本；该样本没有独立 YOLO 标签，只用于工程链路与耗时对比。`
- 实验结论：`单张 228×237 木材样本中，快速整图 116 ms/2 个候选，自适应 353 ms/3 个候选，精细切片 206 ms/3 个候选；同一木材样本缩放到 3000×2000 后能因高分辨率自动升级为 13 个推理区域，耗时 1168 ms，返回 7 个候选且所有框均位于原图边界内。候选数量不等同于正确检测数；该结果只证明木材图片上的链路与策略有效，不能用于声称精度提升。`
- 圆木漏检回归：`用户提供的 1080×810 圆木横截面图片原先只返回左下边缘 1 个裂纹框。修复后 STANDARD 因 EDGE_ONLY_FIRST_PASS 自动执行 3 个区域，自适应耗时 517 ms；保留 1 个模型裂纹结果，并对大圆木中央腐朽空洞、暗部和裂纹区域补充 5 个 suspected_anomaly 候选。补充候选明确要求人工复核，不冒充六类模型预测。`
- 仍需改善：`恢复带标签验证集，按裂纹、小节及目标面积分桶运行 compare_modes.py 和 evaluate.py，计算 Precision、Recall、mAP50、mAP50-95、小目标召回率及多轮耗时；在 CUDA/FP16 环境补充吞吐验收。`

### 5.6 第六阶段 创新功能二 可解释木材质量评分

优先级：P1  
预计时间：3 至 4 个工作日

#### 计划添加内容

- [x] 统计缺陷总数和各类别数量。
- [x] 计算缺陷框面积占图片面积的比例。
- [x] 计算最大缺陷面积比例。
- [x] 为不同缺陷类别配置严重程度权重。
- [x] 生成 0 至 100 的综合质量分。
- [x] 根据配置生成 A、B、C、D 质量等级。
- [x] 展示具体扣分原因。
- [x] 将质量评分规则改为可配置参数。
- [x] 在历史记录中增加质量分和质量等级筛选。
- [x] 在 Excel 和 CSV 中增加质量评价字段。
- [x] 明确说明质量等级属于项目评价规则，不冒充行业强制标准。

#### 质量评分规则预留

> 在实现前填写并评审具体评分规则。

- 基础分：`100 分。`
- 裂纹权重：`每个扣 8 分。`
- 干节权重：`每个扣 6 分。`
- 健全节权重：`每个扣 2.5 分。`
- 边节权重：`每个扣 5 分。`
- 小节权重：`每个扣 2 分。`
- 波纹权重：`每个扣 3 分；疑似异常复核候选每个扣 2 分，未知类别默认每个扣 3 分。`
- 面积惩罚规则：`检测框裁剪到图片边界后计算矩形并集，去重覆盖率每 1% 扣 0.35 分、最多扣 35 分；最大单框面积率每 1% 扣 0.25 分、最多扣 20 分。最终分数限制在 0 至 100，并保留两位小数。`
- 等级划分规则：`A：90–100；B：75–89.99；C：60–74.99；D：0–59.99。`
- 规则依据或参考标准：`WOOD-QS-1.0 项目内部启发式评价规则，用于相对比较和人工复核优先级，不属于或替代任何行业强制标准。类别权重、面积系数、惩罚上限和等级阈值均可通过环境变量配置。`

#### 实施记录和需要改善的内容

- 完成日期：`2026-09-26`
- 实际评分公式：`质量分 = max(0, 100 - Σ(类别数量 × 类别权重) - min(35, 去重覆盖率百分数 × 0.35) - min(20, 最大单框面积率百分数 × 0.25))。评分结果同时保存类别统计、逐项扣分、规则版本和声明快照，避免后续配置变化篡改历史解释。`
- 验证样本数量：`4 组可重复合成边界样本：无缺陷满分、两类重叠框并集面积、越界框裁剪及未知类别权重、A/B/C/D 阈值边界；连同原文件存储测试共 8/8 个后端单元测试通过，前端状态与格式化测试 5/5 通过。`
- 评分合理性结论：`单调性和边界行为符合设计：缺陷数量、覆盖面积或最大缺陷面积增加时得分不会升高；重叠框面积不会重复计算；无缺陷为 100/A；分数始终限制在 0–100。该结论验证工程公式一致性，不代表行业质量等级有效性。`
- 遇到的问题：`项目没有带专家质量等级标注的校准样本，无法证明默认权重与真实生产分级一致；工程链路已完成 Docker 实跑，但评分科学性仍受专家样本缺失限制。`
- 仍需改善：`收集专家评级样本后对权重、面积系数和 A/B/C/D 阈值做相关性及一致性校准；使用真实标注集补充评分筛选和 CSV/Excel 内容的自动化断言。`

### 5.7 第七阶段 创新功能三 主动学习与人工复核闭环

优先级：P2，可作为增强功能  
预计时间：4 至 6 个工作日

#### 计划添加内容

- [x] 用户可以标记检测结果正确或错误。
- [x] 用户可以修正缺陷类别。
- [x] 用户可以删除错误检测框。
- [x] 用户可以补充漏检目标。
- [x] 自动收集低置信度样本。
- [x] 自动收集人工纠错样本。
- [x] 一键导出 YOLO 格式待训练数据包。
- [x] 记录模型版本和人工复核结果。
- [x] 展示不同模型版本的指标变化。
- [x] 建立检测、复核、数据回流和再训练闭环。

#### 实施记录和需要改善的内容

- 完成日期：`2026-09-26`
- 复核流程：`推理服务以“权重文件名-SHA-256 前 12 位”生成模型版本；后端保留原始 detect_detail，按最低模型置信度或 suspected_anomaly 自动入队；用户在历史详情确认、驳回或编辑最终标注；最终标注写入独立 detect_review_annotation 表并记录 MODEL_CONFIRMED / HUMAN_CORRECTED / HUMAN_ADDED 来源；历史页可筛选并查看模型版本反馈指标。`
- 低置信度阈值：`默认 0.45，可通过 ACTIVE_LEARNING_LOW_CONFIDENCE_THRESHOLD 配置；任一六类模型框低于阈值，或存在疑似异常候选时进入待复核队列。`
- 数据导出格式：`YOLO detection ZIP：wood-active-learning/images/train、labels/train、data.yaml、README.txt、manifest.json。仅导出已复核记录，空标签表示人工确认无目标；manifest 保存记录 ID、模型版本、复核状态、复核时间和标注数量。`
- 实际收集样本数：`当前仓库未附带真实用户复核数据，因此未虚构业务样本数；主动学习单元测试已覆盖原框确认、改类、补框、越界拒绝、状态语义、版本变化和 YOLO 标签导出，后端累计 15/15 测试通过，前端累计 6/6 测试通过。`
- 再训练提升情况：`尚未执行真实再训练；缺少原训练/验证集和专家复核样本，无法给出可信的精度提升。工程链路已接到现有 train.py、evaluate.py 和 export_onnx.py，并可在新权重上线后通过版本看板比较人工确认率、纠错率、平均质量分和推理耗时。`
- 仍需改善：`收集足量专家复核样本，按来源和类别分层划分训练/验证集后执行再训练；使用独立验证集比较 mAP、Recall 和小目标召回率；真实 Docker 接口、复核事务和 YOLO ZIP 下载已验证，完整浏览器自动化仍待补充。`

### 5.8 第八阶段 测试 验收与文档交付

优先级：P0  
预计时间：3 至 4 个工作日

#### 计划完成内容

- [x] 增加 Python 单元测试。
- [-] 已增加 Spring Boot 单元测试，数据库集成测试仍待补充。
- [-] 已增加 Vue 状态与格式化测试，组件/API 自动化测试仍待补充。
- [-] 已完成 Playwright 人工验收，自动化端到端测试文件仍待补充。
- [x] 增加 Docker 启动冒烟测试。
- [ ] 建立固定的回归测试图片集合。
- [ ] 编写部署手册。
- [ ] 编写用户使用手册。
- [x] 编写 API 文档。
- [x] 编写模型说明和指标报告。
- [ ] 编写常见问题与故障排查说明。
- [ ] 增加第三方许可证说明。
- [ ] 检查 Ultralytics AGPL-3.0 许可证合规要求。
- [ ] 修订论文和答辩 PPT。
- [x] 建立 Git 仓库和持续集成流程。

#### 跨平台验收矩阵

| 平台 | 模式 | 启动 | 上传识别 | 历史查询 | 导出 | 结果 |
|---|---|---|---|---|---|---|
| Windows 10/11 | CPU | 通过 | 通过 | 通过 | 通过 | 四服务健康；真实图片上传、历史、静态资源、YOLO ZIP、删除和重启持久性通过 |
| Ubuntu x86-64 | CPU | 待测试 | 待测试 | 待测试 | 待测试 | 待填写 |
| Ubuntu + NVIDIA | GPU | 待测试 | 待测试 | 待测试 | 待测试 | 待填写 |
| macOS Intel | CPU | 待测试 | 待测试 | 待测试 | 待测试 | 待填写 |
| macOS Apple Silicon | ONNX CPU | 待测试 | 待测试 | 待测试 | 待测试 | 待填写 |
| Android 浏览器 | 客户端 | 待测试 | 待测试 | 待测试 | 待测试 | 待填写 |
| iOS/iPadOS 浏览器 | 客户端 | 待测试 | 待测试 | 待测试 | 待测试 | 待填写 |

#### 实施记录和需要改善的内容

- 完成日期：`进行中；最近更新 2026-09-26`
- 测试版本：`main 工作区，CPU Compose，模型 best-d5bd6032be1b`
- 通过用例数量：`后端 15、Python 12、前端 6；另通过 Compose 构建、五项自动冒烟及一次真实图片全链路验收`
- 失败用例数量：`0；审计中发现的静态资源 500 和复核状态语义问题均已修复并回归通过`
- 已知问题：`缺少原始训练/验证集；数据库集成、Vue 组件和自动化浏览器 E2E 尚未完成；非 Windows 平台尚待实机验收。`
- 发布限制：`质量分仅为项目内部规则；论文指标尚未用独立验证集复算；正式部署必须修改数据库密码并保持内部调试端口仅本机监听。`
- 最终验收结论：`Windows CPU 工程链路可用，第八阶段部分完成；在数据集、跨平台矩阵和完整自动化测试补齐前不标记最终发布验收完成。`

## 6 建议实施顺序

1. 第一阶段：先解决配置和跨平台启动问题。
2. 第二阶段：修复数据库、文件和错误状态一致性。
3. 第四阶段：统一模型、训练配置和推理环境。
4. 第三阶段：完善前端交互与生产构建。
5. 第五阶段：实现自适应高分辨率检测。
6. 第六阶段：实现可解释质量评分。
7. 第七阶段：在时间允许时实现主动学习闭环。
8. 第八阶段：完成全平台测试、文档和论文修订。

## 7 建议排期

| 周期 | 重点任务 | 计划状态 | 实际状态 |
|---|---|---|---|
| 第 1 周 | 配置重构、数据库迁移、Docker 一键启动 | [-] 部分完成 | Windows CPU 四服务与持久性已实跑通过；Linux、macOS 和 GPU 平台仍待验收 |
| 第 2 周 | 后端可靠性、文件管理、前端异常处理 | [x] 已完成 | 第二阶段后端可靠性和文件管理、第三阶段前端异常体验与响应式改造均已完成 |
| 第 3 周 | 六类模型统一、ONNX 推理、训练与评估脚本 | [-] 部分完成 | 六类部署模型、动态 ONNX 和统一入口已完成；原 6 类与 12 类扩展数据已恢复，12 类训练/复评待执行 |
| 第 4 周 | 自适应切片检测、质量评分 | [x] 已完成 | 第五阶段自适应切片与第六阶段可解释质量评分均已实现并通过单元测试及生产构建验证 |
| 第 5 周 | 主动学习增强、自动化测试和跨平台验收 | [-] 部分完成 | 主动学习闭环、15 个后端/12 个 Python/6 个前端测试、Docker 冒烟和 CI 已完成；完整集成/E2E 与跨平台验收仍待进行 |
| 第 6 周 | 部署文档、模型报告、论文和 PPT 修订 | [ ] 未开始 | 待填写 |

## 8 计划交付物

- [x] 完整且整理后的源码。
- [x] Docker Compose 一键运行方案。
- [x] CPU 和 GPU 两套推理配置。
- [x] `.env.example` 环境变量模板。
- [x] 数据库自动迁移脚本。
- [x] 六类别 PyTorch 模型。
- [x] 六类别 ONNX 模型。
- [x] 可复现训练、验证、导出和性能测试脚本。
- [x] 自适应高分辨率检测功能。
- [x] 可解释质量评分功能。
- [x] 主动学习和人工复核功能，可根据时间作为增强交付。
- [-] 已有前端、后端、Python 单元测试和 CI；完整数据库集成、组件及 E2E 测试仍待补充。
- [ ] 跨平台验收记录。
- [ ] 中文部署手册和用户手册。
- [x] API 文档。
- [ ] 模型说明和实验指标报告。
- [ ] 第三方许可证说明。
- [ ] 修订后的论文和答辩 PPT。

## 9 项目风险与应对措施

| 风险 | 当前状态 | 应对计划 | 后续记录 |
|---|---|---|---|
| 训练数据集缺失 | [x] 已解除 | 找回原始数据集并进行版本化管理 | 已恢复原 6 类并构建 12 类扩展集；大文件本地保存，Git 跟踪脚本、来源和校验报告 |
| 12 类数据不均衡 | [!] 高风险 | 优先自采并人工框注霉变、虫蛀，建立同域独立验证集 | 当前霉变 8 张、虫蛀/虫孔 20 张，只可作为启动集 |
| 论文指标无法复现 | [!] 高风险 | 统一评估脚本并重新生成全部指标 | 待填写 |
| CPU 推理速度较慢 | [-] 待验证 | ONNX Runtime、量化和自适应推理 | 待填写 |
| Apple Silicon 兼容性 | [-] 待验证 | 使用 ONNX CPU 镜像并单独验收 | 待填写 |
| PyTorch CUDA 镜像体积大 | [-] 待改善 | CPU/GPU 镜像分离 | 待填写 |
| 批量任务阻塞接口 | [x] 已缓解 | 后台任务、进度查询和并发限制 | 已新增有界线程池、异步批次接口、总进度、逐项状态、取消、失败项重试，以及推理服务并发闸门和等待超时 |
| 文件和数据库状态不一致 | [x] 已缓解 | UUID、事务补偿和定期清理机制 | 已实现 UUID、上传失败清理和删除暂存/恢复；定期孤儿文件巡检待第八阶段补充 |
| 第三方许可证限制 | [!] 已确认 | 梳理 AGPL-3.0、数据集和模型使用要求 | Oak 为 CC BY-NC 4.0、MVTec 为 CC BY-NC-SA 4.0；当前组合数据仅限非商业研究，商业化前必须替换 |

## 10 缺陷审计与加固记录

### 迭代编号 `AUDIT-2026-09-26`

- 开始日期：`2026-09-26`
- 完成日期：`2026-09-26`
- 迭代目标：`根据计划文件核对当前实现，修复可在仓库内复现的安全、状态语义和 HTTP 错误问题，并补齐可重复验证。`
- 涉及模块：`FastAPI 推理服务、Spring Boot 全局异常/人工复核、Docker Compose、测试与 CI、README。`
- 完成内容：
  - [x] 限制推理服务只读取 `UPLOAD_ROOT` 内的真实文件，阻止路径穿越与符号链接逃逸。
  - [x] 增加推理并发闸门和等待超时，繁忙时返回统一 503 JSON。
  - [x] 统一 FastAPI 参数校验、业务和内部异常的 `success/code/message` 响应，内部错误不再泄露异常细节或服务器路径。
  - [x] 修复已删除/不存在静态资源错误返回 500，现返回统一 404。
  - [x] 修复 `CORRECT` 状态可携带人工修改框导致“结果正确”语义与训练标签不一致的问题。
  - [x] Compose 默认只向局域网开放前端，后端、推理和数据库调试端口仅绑定 `127.0.0.1`。
  - [x] 统一本地开发时 Java 与 Python 的默认上传目录，避免路径边界校验启用后两个进程指向不同目录。
  - [x] 新增 Python 运行时安全/并发测试、后端错误语义与复核状态测试、可重复 Docker 冒烟脚本和 GitHub Actions。
- 未完成内容：
  - [ ] 数据库集成测试、Vue 组件测试与自动化浏览器 E2E。
  - [ ] Linux、NVIDIA、macOS、Android 和 iOS/iPadOS 实机验收。
- 新增问题：
  - `无新增未解决代码缺陷；论文指标尚未独立复算。训练数据集缺失已在后续 DATASET-12-2026-09-26 迭代解除。`
- 测试命令：
  - `mvnw test`、`npm test`、`npm run build`、`npm audit --omit=dev --audit-level=high`
  - `python -m unittest discover -s wood_detect -p "test_*.py" -v`
  - `docker compose up -d --build`、`python scripts/smoke_test.py`
- 测试结果：
  - `后端 15/15、Python 12/12、前端 6/6 通过；生产构建和依赖审计通过；四容器健康；真实图片上传识别、历史/图片读取、复核、YOLO 导出、重启持久性、删除及删除后 404 通过。`

### 迭代编号 `DATASET-12-2026-09-26`

- 开始日期：`2026-09-26`
- 完成日期：`2026-09-26`
- 迭代目标：`恢复原 6 类数据，补齐腐朽、树洞、虫蛀、霉变、树皮脱落、颜色异常六类，并明确排除髓心破坏。`
- 涉及模块：`datasets 数据源缓存、YOLO 数据构建与校验脚本、12 类数据 YAML、训练类别契约。`
- 完成内容：
  - [x] 从 Zenodo 恢复 3785 张原 6 类图片及 YOLO 标注，并在构建时去除 12 张完全重复图。
  - [x] 从 VSB-TUO 提取 `Knot_missing` 为大型空洞、`Blue_Stain/Quartzity` 为颜色异常；未把蓝变误标为霉变，未导入 Marrow。
  - [x] 将 Oak 黑腐/黄腐、spruce 剥落树皮和 MVTec 木材孔洞像素掩码转换为 YOLO 框。
  - [x] 目视筛选 Openverse 腐朽、虫蛀和霉变图片，保留逐图来源/作者/许可，仅作为训练集弱标注。
  - [x] 构建 5857 张、16946 个目标的 12 类数据集；train/val/test 为 4640/930/287。
  - [x] 自动检查图片/标签配对、类别范围、坐标范围、跨集合重复和 12 类非零，最终校验通过且无遗留问题。
  - [x] 将类别 10 从错误的 `bark_pocket` 统一为 `bark_loss`，修正数据路径和旧下载脚本中的错误类别映射。
- 未完成内容：
  - [ ] 霉变与虫蛀仍需同一生产相机域下的大规模人工框标注及独立验证集。
  - [ ] 尚未训练、评估或部署新的 12 类权重；线上仍使用原 6 类模型。
- 许可证结论：`当前组合集含 CC BY-NC 4.0 与 CC BY-NC-SA 4.0 来源，只能用于非商业研究；商业化前必须替换 Oak/MVTec 来源。`
- 验证结果：`5857/5857 图片均有配对标签；12 类均非零；原 6 类和树洞来源中的 26 组完全重复图已去重并合并标注；validation_report.json 状态为 PASS。`

## 11 每次开发迭代记录模板

### 迭代编号 `待填写`

- 开始日期：`待填写`
- 完成日期：`待填写`
- 迭代目标：`待填写`
- 涉及模块：`待填写`
- 完成内容：
  - [ ] `待填写`
- 未完成内容：
  - [ ] `待填写`
- 新增问题：
  - `待填写`
- 改善内容：
  - `待填写`
- 测试命令：
  - `待填写`
- 测试结果：
  - `待填写`
- 相关截图或报告：
  - `待填写`
- 下一步计划：
  - `待填写`

## 11 最终验收结论预留

- 最终版本号：`待填写`
- 发布日期：`待填写`
- 支持的平台：`待填写`
- 默认推理方式：`待填写`
- 已完成创新点：`待填写`
- 模型最终指标：`待填写`
- 已知限制：`待填写`
- 后续改善方向：`待填写`
- 项目最终状态：`待填写`

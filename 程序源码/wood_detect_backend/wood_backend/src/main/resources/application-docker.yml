spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://database:3306/wood_detect?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

file:
  upload-path: ${UPLOAD_PATH:/data/uploads}

python:
  predict-url: ${PYTHON_PREDICT_URL:http://inference:8001/predict}

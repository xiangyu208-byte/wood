spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

file:
  upload-path: ${UPLOAD_PATH}

python:
  predict-url: ${PYTHON_PREDICT_URL}
